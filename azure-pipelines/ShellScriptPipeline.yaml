trigger:
- dev

variables:
  azureSubscriptionServiceConnection: 'acr-spn-connection'
  containerRegistryName: 'azeacrdevpipe'
  containerRegistryLoginServer: 'azeacrdevpipe-fxdye9ebcmczauhv.azurecr.io'
  imageRepository: 'filmagix'
  dockerfilePath: '$(Build.SourcesDirectory)/Dockerfile'
  tag: '$(Build.BuildId)'

pool:
  name: 'docketform-build-agent'

stages:
- stage: Build
  displayName: Build Docker Image
  jobs:
  - job: Build
    displayName: Build Image
    steps:
    - task: AzureCLI@2
      displayName: Build Docker Image using CLI
      inputs:
        azureSubscription: '$(azureSubscriptionServiceConnection)'
        scriptType: bash
        scriptLocation: inlineScript
        inlineScript: |
          echo "Logging into ACR..."
          az acr login --name $(containerRegistryName)

          echo "Building Docker image..."
          docker build -t $(containerRegistryLoginServer)/$(imageRepository):$(tag) -f $(dockerfilePath) .

- stage: Push
  displayName: Push Docker Image to ACR
  dependsOn: Build
  jobs:
  - job: Push
    displayName: Push Image
    steps:
    - task: AzureCLI@2
      displayName: Push Docker Image using CLI
      inputs:
        azureSubscription: '$(azureSubscriptionServiceConnection)'
        scriptType: bash
        scriptLocation: inlineScript
        inlineScript: |
          echo "Logging into ACR..."
          az acr login --name $(containerRegistryName)

          echo "Pushing Docker image to ACR..."
          docker push $(containerRegistryLoginServer)/$(imageRepository):$(tag)

- stage: Update
  displayName: Update Kubernetes Deployment
  dependsOn: Push
  variables:
    - group: updateK8SManifests_variable_group
  jobs:
  - job: Update
    displayName: Update K8s Manifests
    steps:
    - task: ShellScript@2
      displayName: Run updateK8SManifests.sh
      inputs:
        scriptPath: 'scripts/updateK8SManifests.sh'
        args: 'filmagix $(imageRepository) $(tag)'
