apiVersion: apps/v1
kind: Deployment
metadata:
  name: filmagix
  namespace: app
  labels:
    app: filmagix
spec:
  replicas: 3
  selector:
    matchLabels:
      app: filmagix
  template:
    metadata:
      labels:
        app: filmagix
    spec:
      containers:
        - name: filmagix-container
          image: azeacruatpipe-e6hndxgtfafgfad7.azurecr.io/filmagix-uat:248
          ports:
            - containerPort: 8000
              name: filmagix
          envFrom:
            - configMapRef:
                name: filmagix-config
            - secretRef:
                name: filmagix-secrets
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"
      imagePullSecrets:
        -name: filmagix-acr-uat-secret